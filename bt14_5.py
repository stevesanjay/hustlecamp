# Generating all possible combinations of two lists:

colors = ['red', 'green', 'blue']
fruits = ['apple', 'banana', 'cherry']
for color in colors:
    for fruit in fruits:
        print(color, fruit)